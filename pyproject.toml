[project]
name = "nse-trading-dashboard"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "yfinance>=0.2.58",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "requests>=2.31.0",
    "flask>=3.0.0",
    "gunicorn>=21.0.0",
]

[tool.vercel]
entrypoint = "dashboard:app"
